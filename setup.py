from setuptools import setup, find_packages

setup(name='nest',
      version='0.1',
      description='Model Framework',
      author='uber@gerbil.io',
      license='MIT',
      packages=find_packages())
